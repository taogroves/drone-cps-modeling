# Data

Use this directory for city and threat reference data used to justify model parameters.

Suggested subfolders:

- `raw/`: untreated source files (ignored in git by default)
- `processed/`: cleaned summaries safe to commit
- `private/`: restricted datasets (ignored in git)
