# Documentation

Track modeling rationale and analysis outcomes here.

Recommended files:

- `threat-model.md`: Adversary assumptions and attack surfaces
- `experiment-log.md`: Inputs, command run, and key result snapshots
- `decision-records.md`: Why parameter values and mitigations changed
